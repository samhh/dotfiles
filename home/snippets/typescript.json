{
  "desc": {
    "body": [
      "describe(\"${1:name}\", () => {",
      "  $0",
      "})"
    ]
  },
  "fc": {
    "body": [
      "type Props = {",
      "  ${2:props}",
      "}",
      "",
      "export const ${1:name}: FC<Props> = (p) => {",
      "  return (",
      "    $0",
      "  )",
      "}"
    ]
  },
  "it": {
    "body": [
      "it(\"${1:name}\", () => {",
      "  $0",
      "})"
    ]
  },
  "ns": {
    "body": ["import $0* as ${2:namespace} from \"${1:package}\";"]
  },
  "pbt": {
    "body": [
      "fc.assert(fc.property(",
      "  ${1:arbs},",
      "  $0",
      "))"
    ]
  },
  "pbta": {
    "body": [
      "fc.assert(fc.asyncProperty(",
      "  ${1:arbs},",
      "  $0",
      "))"
    ]
  },
  // Snippets are currently only supported globally, hence these
  // project-specific ones. See:
  //   https://github.com/zed-industries/zed/discussions/20647
  "styles": {
    "body": ["import * as styles from \"./${1:component}.module.css\";$0"]
  },
  "uapi": {
    "body": ["import type * as UAPI from \"domain/gen/unsplash\";$0"]
  }
}
